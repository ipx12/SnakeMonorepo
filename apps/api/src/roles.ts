/**
 * Enum of all user roles in the application.
 * Use this instead of raw string literals for role comparisons.
 */
export enum UserRole {
  Admin = 'admin',
  User = 'user',
  Guest = 'guest',
}

