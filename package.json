{
  "name": "snake-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@11.8.0",
  "description": "Boilerplate Turborepo with Next.js and Express.js",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.4"
  }
}
